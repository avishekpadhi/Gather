<turbo-frame id="comment-<%= comment.id %>">
<div id="<%= dom_id comment %>">
  <p class="mt-3 max-w-sm">
                          <span class="font-bold"><%= comment.user&.name %></span>
                          <span class="ml-2 w-2/3   whitespace-normal break-words truncate overflow-x-scroll font-light"><%= comment.body%></span>
  </p>
</div>
</turbo-frame>