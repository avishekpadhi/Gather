<div class="flex w-screen h-screen items-center justify-between">

  <div class="flex h-screen w-1/4 flex-col mt-32 max-h-screen overflow-y-scroll">
        <%# <h5 class="font-bold text-4xl"><%= current_user.name%>
        <%# <h4 class="font-bold text-xl">Users</h4> %>

        



        

        <%= turbo_stream_from "users"%>
        <div id="users">
          <%= render @current_user.followers%>
        </div>
        <%# <h4 class="font-bold text-xl">Rooms</h4> %>
        <%#= turbo_stream_from "rooms"%>
        <%# <div id="rooms"> %>
           <%#= render @rooms%>
        <%# </div> %>
        <%#= render 'layouts/new_room_form'%>
  </div>

  <div class="flex h-[800px] w-2/3  flex-col ml-12 mt-16">
      <% if @single_room %>
        <div id="single_room " class="flex  flex-col">
        <h4 class="font-bold text-4xl"><%=@user&.name || @single_room.name %></h4>
        <%= turbo_stream_from @single_room%>
        <% if @user == current_user %>
        <div id="messages" class="h-[500px] overflow-x-hidden overflow-y-scroll mt-10 mb-5 flex flex-col mr-16">
            <p>Select an user to start texting.</p></div>
        <% else %>
            <div id="messages" class="h-[500px] overflow-x-hidden overflow-y-scroll mt-10 mb-5 flex flex-col mr-16"> <%= render @messages %> </div>
            <%= render 'layouts/new_message_form'%>
        <% end %>

        
        
      <% end %>
  </div>



</div>
