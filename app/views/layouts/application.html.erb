<!DOCTYPE html>
<html>
  <head>
    <title>Gather</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://cdn.tailwindcss.com"></script>
    <%= javascript_importmap_tags %>
    <style>

      <%= ".cont-#{current_user&.id}" %> {
      display: flex;
        /* justify-content: flex-end; */
      align-self: flex-end;
       background-color: silver;

       /* flex-direction: row-reverse; */
      }

      <%= ".msg-#{current_user&.id}" %> {
      /* display: flex; */
        /* justify-content: flex-end; */
        /* align-self: flex-end; */
       background-color: silver;
       flex-direction: row-reverse;
       color: black;
      }

    </style>
  </head>

  <body>
  
    <%= render "layouts/navbar"%>
 
    <main class="">
    <% if !content_for?(:skip_flash_messages) %>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p> 
    <% end %>
       
      <%= yield %>
    </main>
    <script>
        selected = document.getElementById("signup");
        console.log(selected)
        selected_input = document.getElementById("selected_input")
        errorblock = document.getElementById("error_explanation");
        selected_input.addEventListener('change',(event)=>{         
          if (event.target.value){       
              console.log(event.target.value)
              console.log(errorblock,"ha")
              selected.submit() 
          }
        })

        update = document.getElementById("update");
        updated_input = document.getElementById("user_state")
        
        console.log(update)

        updated_input.addEventListener('change',(event)=>{         
          if (event.target.value){       
              console.log(event.target.value)
              
              update.submit() 
          }
        })

        

        


    </script>
  </body>
</html>
